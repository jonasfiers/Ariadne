namespace Ariadne.Core;

/// <summary>
/// Assembly marker for Ariadne.Core — the portable engine the "Neo4j Bolt Connector"
/// OutSystems extension is built from. Real functionality lands feature by feature
/// (parameter mapping, result serialization, connection lifecycle, execution modes).
/// </summary>
public static class AriadneCore
{
    /// <summary>Informational version of the core engine (pre-release scaffold).</summary>
    public const string Version = "0.0.0-dev";
}
